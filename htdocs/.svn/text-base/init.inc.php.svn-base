<?php

session_start();

function bt($oInfos)
{
  echo "<pre style='margin:10px;background:orange;padding:10px;color:black;font-size:12px;'>";
  var_dump($oInfos);
  echo "</pre>";
}

include_once(ROOT_PATH.'htdocs/libs/smarty/Smarty.class.php');

// include_once(ROOT_PATH.'class/load.class.php');
include_once(ROOT_PATH.'class/lang.class.php');
include_once(ROOT_PATH.'class/theme.class.php');
include_once(ROOT_PATH.'class/templates.class.php');
include_once(ROOT_PATH.'class/restapi.class.php');

// include_once(ROOT_PATH.'class/general.func.php');

// include_once(ROOT_PATH.'class/myexception.class.php');

// $oSecurity = new Security();
// $oSecurity->checkAccess(isset($access) ? $access : NULL, isset ($NO_TRACE) ? $NO_TRACE : NULL);

// try {
  // $a = "Lang";
  // call_user_func("__autoload", $a);
  // echo $a. "<br />";
  // $oLang = new Lang();
  //if (!isset($NO_TEMPLATE))
  $oTemplate = new Templates();
// }
// catch (Exception $e) {
//   echo $e->getMessage(), "<br />";
// }

?>